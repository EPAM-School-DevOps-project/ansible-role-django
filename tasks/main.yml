  - name: Copy source code
    copy:
      src: "{{ local_app_path }}"
      dest: "{{ remote_app_path }}"
      force: yes
    
  - name: Install Python Django dependencies
    pip:
      requirements: /opt/django/requirements.txt

  - name: Changing perm of "manage.py", adding "+x"
    file: dest=/opt/django/notejam/manage.py mode=a+x

#   - name: Sync database
#     django_manage: 
#       app_path: "{{ app_path }}"
#       command: syncdb

#   - name: Create superuser
#     django_manage:
#       command: "createsuperuser --noinput --username=Gago --email=hrach.hambaryan@gmail.com"
#       app_path: "{{ app_path }}"

  - name: Copy systemd service file to server
    copy:
      src: /home/hrach-hambaryan/epam-devops-project/django/notejam.service
      dest: /etc/systemd/system
      owner: root
      group: root

  - name: Start notejam service
    service:
      name: notejam.service
      enabled: yes

